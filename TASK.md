# 完整首轮任务

请从零实现“离线版本依赖与特性求解库”，用于包管理内核 / 搜索算法中的本地可复用模块。

1. 从零实现离线包依赖求解库，输入完全在内存的有限注册表；每包最多8个版本、总包数最多20。版本仅非负整数三元组，不实现完整SemVer；约束是下闭上开区间，可多个区间取并。每个包全局最多选择一个版本。
2. 每版本含必需依赖和可选特性；特性激活可增加依赖并请求目标包特性，启用集合沿依赖传递到固定点，允许循环。未启用可选依赖不能混入结果，多个来源请求同包特性取并。
3. 完整求解支持回溯与传播，不能只贪心最高版本；结果满足全部根请求与激活依赖。所有包名按字典序，未选版本视为低于任何版本，选满足约束的全局字典序最大版本向量，确保输入顺序无关。
4. 不可满足时给出根请求的一个子集极小不可满足核：该子集无解，删除任意一个请求后可解；不要求最小基数，返回核内每个请求的原ID，并可用求解器逐项验证。限制搜索节点，超限返回unknown，不伪报unsat。
5. 输出确定锁定清单，含版本、最终特性、激活依赖来源；独立检查器验证清单，无网络、安装器或真实包仓库。用小注册表穷举候选版本与特性闭包交叉验证。

验收重点：最高版本死路回退、菱形冲突、循环依赖、特性循环、可选依赖不激活、极小核、unknown预算、根顺序无关。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Python 3.14.7标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；python demo.py须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为python -m unittest discover -s tests -v。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
