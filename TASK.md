# 完整首轮任务

构建系统在完全离线注册表中解析版本和特性，生成可复核的依赖锁单。请从零实现“离线版本依赖与特性求解库”。

1. 实现离线求解库，至多12包、每包6版本，版本为非负整数三元组，不实现完整SemVer。依赖区间为多个下闭上开区间取并，同包多来源约束取交；每包只能选一个版本。根请求有唯一ID。
2. 每版本有必需依赖和命名特性，特性可增加依赖及目标特性请求；启用集合传播到固定点并允许循环。同包特性取并，没有默认特性；该版本无被请求特性时该候选不可行。锁单恰好是根与激活依赖的最小可达闭包，不加无关包。
3. 传播后按最小包名选择未决包、版本按降序尝试，冲突时回溯并撤销该分支特性/依赖影响，返回该确定搜索顺序的首个解。无需全局最优版本向量，不能贪心跳过回溯。
4. 搜索节点预算为整个请求共用，每次尝试一个包版本计一节点，耗尽返回unknown，只有完整排除所有分支才返回unsat；可行返回sat与版本/最终特性/依赖来源。unsat附根ID与冲突诊断，不要求极小核，不把局部冲突冒充完整无解证明。
5. 实现独立锁单检查器核对根、版本、特性固定点及无无关包；用<=4包的小表穷举交叉验证sat/unsat。拒绝重复包/版本/根ID和不存在的包引用；无网络、安装器、真实仓库或额外优化器。

验收重点：最高版本死路回退、菱形冲突、循环特性、回溯撤销、未知特性候选拒绝、无关包不入锁单、预算unknown。

工程约束：全新0-1核心库，仅用Python 3.14.7标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`python demo.py`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`python -m unittest discover -s tests -v`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
