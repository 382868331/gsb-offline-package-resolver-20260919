# 完整首轮任务

写一个离线依赖求解库，输入是小型包注册表和根请求，输出是可以独立检查的锁单。版本选择和特性传播可能相互影响，需要支持回溯；搜索规则如下。

实现离线求解库，至多12包、每包6版本，版本为非负整数三元组，不实现完整SemVer。依赖区间为多个下闭上开区间取并，同包多来源约束取交；每包只能选一个版本。根请求有唯一ID。

每版本有必需依赖和命名特性，特性可增加依赖及目标特性请求；启用集合传播到固定点并允许循环。同包特性取并，没有默认特性；该版本无被请求特性时该候选不可行。锁单恰好是根与激活依赖的最小可达闭包，不加无关包。

传播后按最小包名选择未决包、版本按降序尝试，冲突时回溯并撤销该分支特性/依赖影响，返回该确定搜索顺序的首个解。无需全局最优版本向量，不能贪心跳过回溯。

搜索节点预算为整个请求共用，每次尝试一个包版本计一节点，耗尽返回unknown，只有完整排除所有分支才返回unsat；可行返回sat与版本/最终特性/依赖来源。unsat附根ID与冲突诊断，不要求极小核，不把局部冲突冒充完整无解证明。

实现独立锁单检查器核对根、版本、特性固定点及无无关包；用<=4包的小表穷举交叉验证sat/unsat。拒绝重复包/版本/根ID和不存在的包引用；无网络、安装器、真实仓库或额外优化器。

测试里请覆盖这些情况：最高版本死路回退、菱形冲突、循环特性、回溯撤销、未知特性候选拒绝、无关包不入锁单、预算unknown。

使用 Python 3.14.7 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `python demo.py` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `python -m unittest discover -s tests -v` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
